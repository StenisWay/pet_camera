[project]
name = "pet-camera-db"
version = "0.1.0"
description = "Pet Camera 共用 PostgreSQL schema:SQLAlchemy 2.0 model 與 Alembic migration"
requires-python = ">=3.12"
dependencies = [
    "sqlalchemy>=2.0.36",
    "alembic>=1.14.0",
    "psycopg[binary]>=3.2.3",
]
